dependencies {
    file("$rootDir/core").eachDir { dir ->
        implementation project(path: ":$dir.name")
    }
    file("$rootDir/data").eachDir { dir ->
        implementation project(path: ":$dir.name")
    }
}